{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon src/main.ts",
    "test": "npm run db:deletetable && npm run db:createtable && jest",
    "docker:build": "docker build -t lara_backend docker",
    "docker:run": "docker run -it -v $(pwd)/.:/app --name lara_container lara_backend bash",
    "docker:start": "docker start -i lara_container",
    "docker:stop": "docker stop lara_container",
    "docker:exec": "docker exec -i -t lara_container bash",
    "compose:up": "docker compose -f ./docker/docker-compose.yaml up -d --build",
    "compose:down": "docker compose -f ./docker/docker-compose.yaml -v down",
    "compose:exec": "docker compose -f docker/docker-compose.yaml exec -it lara_backend bash",
    "db:createtable": "npx ts-node src/infrastructure/dynamodb/tables/CreateUserTable.ts",
    "db:deletetable": "npx ts-node src/infrastructure/dynamodb/tables/DeleteUsersTable.ts"
  },
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "@types/bcrypt": "^6.0.0",
    "@types/express": "^5.0.3",
    "@types/jest": "^30.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "axios": "^1.11.0",
    "eslint": "^9.34.0",
    "jest": "^30.0.4",
    "nodemon": "^3.1.10",
    "ts-jest": "^29.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.42.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.872.0",
    "@aws-sdk/lib-dynamodb": "^3.872.0",
    "bcrypt": "^6.0.0",
    "dotenv": "^17.0.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "tslib": "^2.8.1"
  }
}
